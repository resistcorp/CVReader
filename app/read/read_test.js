'use strict';

describe('myApp.read module', function() {

  beforeEach(module('myApp.read'));

  describe('read controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var readCtrl = $controller('ReadCVCtrl');
      expect(readCtrl).toBeDefined();
    }));

  });
});